# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- main

pool:
  vmImage: ubuntu-latest


steps:
- task: NodeTool@0

  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'
 



- script: |
    npm install
    npm run build
    npm install -g newman
    newman -v
    npm install -g newman-reporter-htmlextra
    newman run The_Star_Wars_API.postman_collection.json
    npm install -g newman-reporter-htmlextra
    newman run The_Star_Wars_API.postman_collection.json -r htmlextra –reporter-htmlextra-export ./newman/Relatorio_The_Star_Wars_API.html


  displayName: 'Teste de API Star War by Glaucio'
